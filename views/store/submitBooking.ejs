<%-include('../partials/head')%>

<style>
    body {
        background: #f4f4f9; /* Light grayish background for a clean look */
        font-family: 'Poppins', sans-serif;
        margin: 0;
        padding: 0;
        color: #333; /* Darker text for better readability */
    }

    h1, h2 {
        animation: fadeIn 1.5s ease-in-out;
    }

    h1 {
        color: #4CAF50; /* Green for success */
        text-align: center;
        font-size: 3em;
        margin-top: 40px;
        text-shadow: 1px 1px 3px rgba(0, 0, 0, 0.2);
    }

    h2 {
        color: #555; /* Subtle gray for secondary text */
        text-align: center;
        font-size: 1.8em;
        margin-top: 15px;
        text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.1);
    }

    button {
        background: #4CAF50; /* Green button */
        color: white;
        padding: 15px 40px;
        border: none;
        border-radius: 50px;
        font-size: 1.2em;
        cursor: pointer;
        transition: transform 0.3s ease, background-color 0.3s ease, box-shadow 0.3s ease;
        box-shadow: 0 4px 10px rgba(76, 175, 80, 0.4);
    }

    button:hover {
        background: #45a049; /* Slightly darker green on hover */
        transform: scale(1.1);
        box-shadow: 0 6px 15px rgba(69, 160, 73, 0.5);
    }

    .button-container {
        text-align: center;
        margin-top: 30px;
        animation: slideIn 1.5s ease-in-out;
    }

    @keyframes fadeIn {
        from {
            opacity: 0;
        }
        to {
            opacity: 1;
        }
    }

    @keyframes slideIn {
        from {
            transform: translateY(50px);
            opacity: 0;
        }
        to {
            transform: translateY(0);
            opacity: 1;
        }
    }

    a {
        text-decoration: none;
    }
</style> 

<body>
    <h1>Payment Successfully Completed</h1>
    <h2>Your Home is Now Booked</h2>
    <h2>Thanks for Booking!</h2>
    <h2>You Can Check Your Booking Details in the Reserve Section</h2>
    <div class="button-container">
        <a href="/user/reserve">
            <button>
                Go to Reserve Section
            </button>
        </a>
    </div>
</body>
</html>
